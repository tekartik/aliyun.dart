# Setup

`pubspec.yaml`:

```yaml
dependencies:
  tekartik_aliyun_oss_node:
    git:
      url: git://github.com/tekartik/aliyun.dart
      path: node/oss_node
      ref: null_safety
    version: '>=0.1.0'
```