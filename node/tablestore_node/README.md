# Setup

`pubspec.yaml`:

```yaml
dependencies:
  tekartik_aliyun_tablestore_node:
    git:
      url: git://github.com/tekartik/aliyun.dart
      path: node/tablestore_node
      ref: dart2
    version: '>=0.1.0'
```