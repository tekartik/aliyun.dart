name: app_node_utils_repo_support
description: Mustache repo (tools)
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ">=6.1.0"
  dev_build: ">=1.1.7+3"
  path: ">=1.9.1"
  pub_semver: ">=2.2.0"
  process_run: ">=1.2.1+1"

resolution: workspace

